<%= form_with model: [post, post_comment], url: users_post_post_comments_path(post) do |f| %>
  <div class="row">
    <div class="col-sm-6">
      <%= f.text_area :comment, rows:'5', class: "form-control",placeholder: "コメントをここに" %>
    </div>
  </div>
      <%= f.submit "送信する" %>
<% end %>